# Process chat exports in the browser

Raw chat exports are processed only in the browser and are never uploaded or retained by Top Map Tap. The creation request sends only the minimal fields from surviving import candidates needed for authoritative server validation, because uploading an entire export would expose unrelated private conversation without providing domain value.
